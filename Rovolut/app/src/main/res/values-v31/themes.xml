<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="Theme.Rovolut" parent="Theme.Material3.DayNight.NoActionBar">

        <!-- Transparent statusBar: -->
        <item name="android:windowTranslucentStatus">false</item>
        <item name="android:windowDrawsSystemBarBackgrounds">false</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:fitsSystemWindows">false</item>

        <!-- Splash Screen animation -->
        <item name="android:windowSplashScreenBackground">@color/black</item>
        <item name="android:windowSplashScreenIconBackgroundColor">@android:color/transparent</item>
        <item name="android:windowSplashScreenAnimatedIcon">@drawable/anim_splash</item>
        <item name="android:windowSplashScreenAnimationDuration">1400</item>

    </style>

</resources>